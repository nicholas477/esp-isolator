[package]
name = "esp-isolator"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
egg-esp-lib = "0.1.0"
niflib = "0.1.0"

[patch.crates-io]
egg-esp-lib = { path = "./egg-esp-lib" }
niflib = { path = "./niflib" }
